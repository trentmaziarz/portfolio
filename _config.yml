title: "Trent Maziarz"
description: "Computer science, Buhurt, and the family grease."
url: "https://trentmaziarz.com"
baseurl: ""

collections:
  projects:
    output: false

defaults:
  - scope:
      path: ""
    values:
      layout: default
  - scope:
      path: "_posts"
    values:
      layout: post
  - scope:
      path: ""
      type: "projects"
    values:
      layout: project

markdown: kramdown
kramdown:
  input: GFM

plugins:
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-redirect-from

exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - _archive
